Let's give Rust a shot.

My setup will be simply install it via:
```bash
curl --proto '=https' --tlsv1.2 -sSf https://sh.rustup.rs | sh
```

And create and code every file directly in the terminal.
```bash
vi filename.rs
```
